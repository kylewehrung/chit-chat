version: 1
frontend:
 phases:
    preBuild:
      commands:
        - cd client
        - npm ci
    build:
      commands:
        - npm run-scripts build
 artifacts:
    baseDirectory: build
    files:
      - '**/*'
 cache:
    paths:
      - node_modules/**/*
